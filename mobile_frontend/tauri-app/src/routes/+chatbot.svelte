// chatbot.svelte will have a header, input(text, picture, files, and audio recorder(research this)), userMessage bubble enabled for copy/download, a claude code/chatgpt inspired botMessage component
